@model List<About>
@{
    ViewData["Title"] = "About";
    int count = 1;
}
<div class="container">
    <div class="row">
        <div class="header d-flex justify-content-between mt-3">
            <h3>About</h3>
            <div>
                <a class="btn btn-success disabled" asp-controller="About" asp-action="Create">Create</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Number
                        </th>
                        <th>
                            Title
                        </th>
                        <th>
                            Settings
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @count
                            </td>
                            <td>
                                <p>@item.Title</p>
                            </td>
                            <td>
                                <a class="btn btn-primary" asp-action="Detail" asp-route-id="@item.Id">
                                    Detail
                                </a>
                                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">
                                    Edit
                                </a>

                            </td>
                        </tr>
                        count++;
                    }

                </tbody>
            </table>
        </div>
    </div>

</div>


