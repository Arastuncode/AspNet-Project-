@model Skill
@{
    ViewData["Title"] = "Detail";
}

<div class="container-fluid mt-2 text-center">
    <h3>Teacher skill detail</h3>
    <div class="row mt-5">
        <h5>@Model.Name</h5>
    </div>
    <a asp-action="Index" class="btn btn-primary mt-3">Go back</a>
</div>

